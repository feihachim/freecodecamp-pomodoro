import breakAction from "./breakAction";
import sessionAction from "./sessionAction";
import timerAction from "./timerAction";

const allActions = { breakAction, sessionAction, timerAction };

export default allActions;
